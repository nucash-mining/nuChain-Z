{
  "name": "nft-mining-rig-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for NFT Mining Rig configuration and Mining Pool operations",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:altcoinchain": "hardhat run scripts/deploy.js --network altcoinchain",
    "deploy:polygon": "hardhat run scripts/deploy.js --network polygon",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "verify:altcoinchain": "hardhat verify --network altcoinchain",
    "verify:polygon": "hardhat verify --network polygon",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@openzeppelin/contracts": "^4.9.0",
    "hardhat": "^2.17.0",
    "dotenv": "^16.3.0"
  },
  "dependencies": {
    "ethers": "^5.7.2"
  }
}